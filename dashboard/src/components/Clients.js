import { positions } from "@mui/system";
import React from "react";

function Clients() {
  return (
    <div
      style={{
        color: "green",
        position: "absolute",
        top: "10rem",
        left: "40%",
        fontSize: "2rem",
      }}
    >
      this is Clients page!{" "}
    </div>
  );
}

export default Clients;
